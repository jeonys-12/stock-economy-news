const qualityValueState={items:[],query:'',grade:'all'};
function qvEscape(value){return String(value??'').replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));}
function qvNumber(value,suffix=''){if(value===null||value===undefined||value==='')return'-';const number=Number(value);return Number.isFinite(number)?`${number.toLocaleString('ko-KR',{maximumFractionDigits:2})}${suffix}`:'-';}
function qvCompare(value,peer,lowerIsBetter=false){const a=Number(value),b=Number(peer);if(!Number.isFinite(a)||!Number.isFinite(b))return'비교 자료 없음';const better=lowerIsBetter?a<=b:a>=b;return`${better?'업종 중앙값 이상':'업종 중앙값 미달'} · 업종 ${qvNumber(b,lowerIsBetter?'배':'')}`;}
function qvRows(payload){const stocks=payload?.stocks&&typeof payload.stocks==='object'?payload.stocks:{};return Object.entries(stocks).map(([name,row])=>{const analysis=row.quality_value_analysis||{};return{name,row,analysis,metrics:analysis.metrics||{},peer:analysis.peer_medians||{},score:Number(analysis.score||0),grade:analysis.grade||'D'};}).sort((a,b)=>b.score-a.score||a.name.localeCompare(b.name,'ko'));}
function qvRender(){const list=document.querySelector('#qualityValueList'),summary=document.querySelector('#qualityValueSummary');if(!list||!summary)return;const query=qualityValueState.query.trim().toLowerCase();const filtered=qualityValueState.items.filter(item=>{const text=`${item.name} ${item.row.code||''} ${item.row.business_sector||item.row.sector||''}`.toLowerCase();return(qualityValueState.grade==='all'||item.grade===qualityValueState.grade)&&(!query||text.includes(query));});const high=qualityValueState.items.filter(item=>item.score>=65).length;summary.innerHTML=`전체 <b>${qualityValueState.items.length}</b>개 · B등급 이상 <b>${high}</b>개 · 현재 표시 <b>${filtered.length}</b>개 · 네이버 Financial Summary 기준`;if(!filtered.length){list.innerHTML='<p class="result-info">조건에 맞는 종목이 없습니다.</p>';return;}list.innerHTML=filtered.map(item=>{const m=item.metrics,p=item.peer;const gradeClass=item.grade==='A'||item.grade==='B'?'positive':item.grade==='D'?'negative':'neutral';const history=(item.analysis.history||[]).map(row=>`<tr><td>${qvEscape(row.year)}</td><td>${qvNumber(row.revenue)}</td><td>${qvNumber(row.operating_profit)}</td><td>${qvNumber(row.eps)}</td><td>${qvNumber(row.roe_pct,'%')}</td><td>${qvNumber(row.debt_ratio_pct,'%')}</td></tr>`).join('');return`<details class="qv-card"><summary><div><strong>${qvEscape(item.name)}</strong><span>${qvEscape(item.row.code||'')} · ${qvEscape(item.analysis.peer_sector||item.row.sector||'업종 미분류')}</span></div><div class="qv-summary-metrics"><span>EPS 성장 ${qvNumber(m.eps_growth_cagr_pct,'%')}</span><span>영업이익 성장 ${qvNumber(m.operating_profit_growth_cagr_pct,'%')}</span><span>평균 ROE ${qvNumber(m.average_roe_pct,'%')}</span><span>추정 PER ${qvNumber(m.forward_per,'배')}</span></div><div><span class="qv-grade ${gradeClass}">${qvEscape(item.grade)}등급</span><b>${qvNumber(item.score,'점')}</b></div></summary><div class="qv-detail"><p class="qv-note"><strong>출처:</strong> ${qvEscape(item.analysis.source||'NAVER Finance Financial Summary')} · 최근 3~5년 추세와 동일 업종 중앙값 비교</p><div class="qv-metric-grid"><article><span>EPS 3~5년 성장률</span><strong>${qvNumber(m.eps_growth_cagr_pct,'%')}</strong><small>${qvEscape(qvCompare(m.eps_growth_cagr_pct,p.eps_growth_cagr_pct))}</small></article><article><span>영업이익 3~5년 성장률</span><strong>${qvNumber(m.operating_profit_growth_cagr_pct,'%')}</strong><small>${qvEscape(qvCompare(m.operating_profit_growth_cagr_pct,p.operating_profit_growth_cagr_pct))}</small></article><article><span>3~5년 평균 ROE</span><strong>${qvNumber(m.average_roe_pct,'%')}</strong><small>${qvEscape(qvCompare(m.average_roe_pct,p.average_roe_pct))}</small></article><article><span>최근 부채비율</span><strong>${qvNumber(m.latest_debt_ratio_pct,'%')}</strong><small>${qvEscape(qvCompare(m.latest_debt_ratio_pct,p.latest_debt_ratio_pct,true))}</small></article><article><span>추정 PER</span><strong>${qvNumber(m.forward_per,'배')}</strong><small>${qvEscape(qvCompare(m.forward_per,p.forward_per,true))}</small></article></div><div class="qv-components">${Object.entries(item.analysis.components||{}).map(([key,value])=>`<span>${qvEscape({eps_growth:'EPS 성장',operating_profit:'영업이익 성장',roe:'ROE',financial_safety:'재무안전성',valuation:'밸류'}[key]||key)} ${qvNumber(value,'점')}</span>`).join('')}</div><div class="qv-table-wrap"><table><thead><tr><th>연도</th><th>매출액</th><th>영업이익</th><th>EPS</th><th>ROE</th><th>부채비율</th></tr></thead><tbody>${history||'<tr><td colspan="6">연간 데이터 없음</td></tr>'}</tbody></table></div><p class="qv-note">${qvEscape((item.analysis.reasons||[]).join(' · ')||'분석 근거가 충분하지 않습니다.')}</p></div></details>`;}).join('');}
async function qvLoad(){const list=document.querySelector('#qualityValueList');if(list)list.innerHTML='<p class="result-info">품질·가치 분석 데이터를 불러오는 중입니다.</p>';try{const response=await fetch(`data/stock_data.json?t=${Date.now()}`,{cache:'no-store'});if(!response.ok)throw new Error(`HTTP ${response.status}`);qualityValueState.items=qvRows(await response.json());qvRender();}catch(error){if(list)list.innerHTML=`<p class="result-info">분석 데이터를 불러오지 못했습니다: ${qvEscape(error.message)}</p>`;}}
document.addEventListener('DOMContentLoaded',()=>{document.querySelector('#qualityValueFilters')?.addEventListener('click',event=>{const button=event.target.closest('button');if(!button)return;document.querySelectorAll('#qualityValueFilters button').forEach(item=>item.classList.toggle('active',item===button));qualityValueState.grade=button.dataset.grade||'all';qvRender();});document.querySelector('#qualityValueSearch')?.addEventListener('input',event=>{qualityValueState.query=event.target.value;qvRender();});qvLoad();});
